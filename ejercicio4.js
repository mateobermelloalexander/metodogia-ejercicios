const person = { name: 'John', age: 30, city: 'New York' };

// Asignación de desestructuración para extraer valores del objeto persona
const { name, age, city } = person;

// Mostrando los resultados usando alert
alert(name); // Output: John
alert(age);  // Output: 30
alert(city); // Output: New York
